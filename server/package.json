{
  "name": "tv-portfolio-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Secure backend for the portfolio private area. Authenticates against WordPress and proxies private lead data with server-side authorization.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "mock-wp": "node scripts/mock-wp.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  }
}