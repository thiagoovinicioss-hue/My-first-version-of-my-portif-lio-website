{
  "name": "tv-portfolio-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Secure backend for the portfolio private area. Validates Supabase Auth tokens server-side, enforces ADMIN_USER_ID authorization, and proxies private lead data with the service-role key.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}